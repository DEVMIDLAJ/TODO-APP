import 'package:flutter/cupertino.dart';

class CalendarPge extends StatelessWidget {
  const CalendarPge({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Calendar screen"));
  }
}